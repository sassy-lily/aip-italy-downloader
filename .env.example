# Copy to .env and fill in. .env is gitignored — never commit real credentials.

# ENAV account (free registration required to access the AIP service).
AIP_USER=
AIP_PASS=

# Portal base URL and where snapshots are written.
AIP_BASE_URL=https://www.enav.it
AIP_OUTPUT_DIR=./downloads
